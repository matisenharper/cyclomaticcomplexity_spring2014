package cyclomaticComplexity;

public class CyclomaticComplexityCalc {
public static int CyclomaticComplexityCalc(GraphModel gm)
{
	//int node = gm.getNodeCount();
	//int edge = gm.getEdgeCount();
	System.out.println("Calculate cyclomatic complexity using node count, edge count, and formula");
	return 0;
}
}
